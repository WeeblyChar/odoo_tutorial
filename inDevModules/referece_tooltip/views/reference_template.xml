<odoo>
    <template id="reference_demo" name="Reference Tooltip Demo">
        <t t-call="website.layout">
            <div class="container">
                <p>
                    This is some text with a reference
                    <sup class="reference" data-ref="ref1">[1]</sup>.
                </p>
                <div id="ref1" class="reference-popup">
                    <strong>Reference Title:</strong> Example Book<br/>
                    <em>Author:</em> John Doe<br/>
                    <em>Year:</em> 2024
                </div>
            </div>
        </t>
    </template>

    <!-- Inline CSS (No External File) -->
    <template id="reference_css" inherit_id="web.assets_frontend">
        <xpath expr="." position="inside">
            <style>
                .reference-popup {
                    position: absolute;
                    background: white;
                    border: 1px solid #ccc;
                    padding: 8px;
                    display: none;
                    z-index: 1000;
                    width: 200px;
                    font-size: 14px;
                    box-shadow: 2px 2px 10px rgba(0,0,0,0.1);
                }
                .reference {
                    color: blue;
                    cursor: pointer;
                }
            </style>
        </xpath>
    </template>

    <!-- Inline JS (No External File) -->
    <template id="reference_js" inherit_id="web.assets_frontend">
        <xpath expr="." position="inside">
            <script type="text/javascript">
                document.addEventListener("DOMContentLoaded", function () {
                    document.querySelectorAll(".reference").forEach(ref => {
                        ref.addEventListener("mouseenter", function (event) {
                            let refId = event.target.getAttribute("data-ref");
                            let popup = document.getElementById(refId);
                            if (popup) {
                                popup.style.left = event.pageX + "px";
                                popup.style.top = event.pageY + "px";
                                popup.style.display = "block";
                            }
                        });

                        ref.addEventListener("mouseleave", function () {
                            let refId = event.target.getAttribute("data-ref");
                            let popup = document.getElementById(refId);
                            if (popup) {
                                popup.style.display = "none";
                            }
                        });
                    });
                });
            </script>
        </xpath>
    </template>
</odoo>
